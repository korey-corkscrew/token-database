{
  "name": "token-database",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ korey-corkscrew/token-database",
    "create-local": "graph create --node http://localhost:8020/ korey-corkscrew/token-database",
    "remove-local": "graph remove --node http://localhost:8020/ korey-corkscrew/token-database",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 korey-corkscrew/token-database"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.30.1",
    "@graphprotocol/graph-ts": "0.27.0"
  }
}
